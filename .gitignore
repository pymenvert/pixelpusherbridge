# Compilation
build/
*.class
sources_list.txt

# Binaires générés (à distribuer via GitHub Releases, pas dans le dépôt).
# Tout dist/ est ignoré : les règles par motif laissaient passer les jars
# imbriqués (dist/PixelPusher Bridge (Windows)/PixelPusherBridge.jar).
dist/

# Système
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.iml
.project
.classpath
